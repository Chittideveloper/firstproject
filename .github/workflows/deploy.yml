name: Test Workflow

on:
  push:
    branches: [ main ]   # change to master if needed
  workflow_dispatch:

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "GitHub Actions is working!"